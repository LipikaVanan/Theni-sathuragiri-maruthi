# AutoCare Pro - Server

## Setup

1. Copy `.env` and set `MONGO_URI` and `JWT_SECRET`.
2. Install dependencies:

```bash
cd server
npm install
```

3. Run in development:

```bash
npm run dev
```

API endpoints are under `/api/*` as scaffolded.
